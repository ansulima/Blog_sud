<h1>Articles</h1>

<%= link_to 'Novo Article', new_article_path %>

<ul>
    <% @articles.each do |article| %>
        <li> 
        <a href="<%= article_path(article) %>">
            <%= article.title %>   
        </li>
    <% end %>
</ul>
